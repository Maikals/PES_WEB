<?php

class QuioscsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('quioscs')->truncate();

		$quioscs = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('quioscs')->insert($quioscs);
	}

}
