<?php

class QuiosquersTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('quiosquers')->truncate();

		$quiosquers = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('quiosquers')->insert($quiosquers);
	}

}
